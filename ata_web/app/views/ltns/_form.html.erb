<%= form_with(model: ltn) do |form| %>
  <% if ltn.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ltn.errors.count, "error") %> prohibited this ltn from being saved:</h2>

      <ul>
        <% ltn.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :scheme_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
